# BrainPlay v4.60 Phase 63

本版以 main 的 `f746573fa80cfbf42d60a32f9e43b14431c5da5c` 為基底。使用者附件與該版本 `index.html` 的 Git blob 都是 `e99638b9f2cc3a9b0907e87b2e15d604cc49596f`，因此不需要在兩個不同首頁版本之間取捨。

## 題庫與分級

正式英文題庫共 **7,000 個不重複詞條**，另保留 **500 個片語**。移除 593 個舊項目、補入 1,593 個項目；舊詞條也有重新分級與釋義／用法修正。完整移除理由與數量在 `data/english-audit.json`。

| 階段 | 適合的練習方向 | 詞條數 |
| --- | --- | ---: |
| 起步 | 生活實物、基本動作、句子基礎 | 657 |
| 基礎 | 家庭、校園與生活用語 | 1,860 |
| 成長 | 日常溝通與常用描述 | 1,816 |
| 進階 | 閱讀與表達 | 1,195 |
| 延伸 | 學習與考試 | 883 |
| 挑戰 | 較廣泛的閱讀詞彙 | 589 |

階段是本產品的編輯分層，不是年齡測驗，也不是經過認證的 CEFR 對照。沒有為了每級字數相同而把簡單字推到最高級。起步階段先呈現具體生活詞；每階段再分成最多 25 字的小單元，可按主題篩選。

此次修正包含：把 `cute`、`zero` 等移到起步，補齊 `the`、`and`、`go` 等功能字；移除 `interpolation`、`colitis`、`antibody` 等專業項目與非詞條內容；移除未有可靠修訂的星期套版例句；修正 `fat`、`interior`、`default` 等詞義／詞性與用法不一致之處。

同一詞條先教一個選定常用義，不等同完整字典。真正的複合詞可以保留連字號；多字片語不拿來湊單字數。英美拼法若有兩種收錄則合併，舊收藏載入時會轉到保留的拼法。`data/english-aliases.json` 保存此對應。

新增英文內容包含短例句與自然搭配，畫面明確標示「用法：例句或搭配」。初學用法不強迫寫成長句；相同常用搭配框架可以重複，檢查重點是詞義、詞性與搭配是否合理，而非靠加入星期或地點來製造表面不重複。

## 學習方式

- 預設先看字卡，測驗預設 5 題，可選 1–20 題。
- 看過字卡後，可只練習剛看過的最多 20 個字。
- 同回合不重複詞條；選項來自同階段，避開重複字與相同中文釋義。
- 起步選義使用兩個選項，後續階段使用三個；填空逐步增加缺字。
- 作答後顯示答案、中文、詞性與用法，由使用者按下一題。
- 答錯立即加入複習；連續答對後安排 1、3、7、14 天再複習。這是簡單間隔複習排程，不宣稱個人化學習成效已經實驗驗證。
- 可重練本次錯題、只練收藏字或到期字。空的篩選結果不會偷偷改出全題庫。
- 正常／慢速發音適用字卡與測驗；無法播放聲音時，可切換同題的文字練習。
- 英文成績以正確率為主，不因答得慢而扣分；暫停時間不納入逐題作答時間。
- 隨機挑戰的四級英文題分別取自前四個學習階段。500 個片語仍維持獨立四級，各 125 個。
- 學習進行中收起設定，讓手機優先顯示字卡或題目。

## 資料與驗證範圍

`data/english-foundations.tsv` 與 `data/english-additions.txt` 是本次編寫的教學內容。`data/english-frequency.json` 僅保存從 [ECDICT](https://github.com/skywind3000/ECDICT) 取出的 BNC 詞頻序位，輔助排序和人工篩選；沒有將原字典的中文釋義或英文定義自動倒入卡片。ECDICT 的 MIT 授權附於 `data/ECDICT-LICENSE.txt`。詞頻本身不決定兒童難度。

保留舊題庫的部分內容與新增內容一起接受全量完整性、去重、用法包含目標字、填空可解性與選項檢查，並針對發現的語義問題修訂。這不等於第三方語言教師逐字認證；後續可依孩子的實際錯題與教師回饋繼續調整分層與多義字。

`tests/regression.cjs` 涵蓋 21,000 次填空生成、7,000 個詞的選項、36 組階段／模式操作、片語、收藏、錯題與複習、暫停、雙語介面、手機寬度、其他遊戲畫面與離線載入。原有數學、規律、數獨及滑塊的資料檢查仍執行。原版日文有五種因形容詞活用而產生的檢查警告，本次不修改日文內容。

瀏覽器測試的語音以可控的語音物件確認文字、速度與流程；實體裝置的聲音品質、iOS 系統語音及真實兒童學習成效仍需實機／教學觀察。

## 開發與重建

需要 Node.js、Git、Chrome 及 `npm install` 安裝的 Playwright。`npm test` 執行回歸，結果寫入未追蹤的 `tests/results.json`，並產生手機／桌面畫面。可用 `BRAINPLAY_BROWSER` 指定已安裝的 Playwright 瀏覽器 channel。

`npm run build` 從已核對的 Phase 62 commit 與本版資料重建 `index.html`，因此需要包含該 commit 的 Git 歷史。它也更新 `sw.js` 快取版號。部署仍只需要首頁與既有靜態資源，不需要 Node.js、外部資料服務或線上字典。

歷史 `educational_puzzle_games_v4_phase61.html` 保留作為舊版檔案；正式入口為 `index.html`。新版快取只清除 BrainPlay 自己的舊快取。
